# Required: PostgreSQL connection string (used by Drizzle and the API)
# Local example (default user/password/db):
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/postgres

# Optional: port for the server (default 5001)
# PORT=5001
